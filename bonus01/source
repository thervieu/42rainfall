int main(int ac, char **av) {
    int value = atoi(av[1]);
    if (value <= 9) {
        char *str;
        memcpy(str, av[2], value * 4)
        if (value == 1464814662) {
            execl("/bin/sh", {"/bin/sh", NULL});
        }
    }
    return (0);
}