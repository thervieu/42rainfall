binary takes one argument

check if atoi(av[1]) == 0

entirely freads "/home/user/end/.pass"
then freads it again and compares that string with av[1]
but since it already read it once the file stream is on the eof

./bonus ""
we are inside a shell

works because "" = "" and atoi("") = 0
