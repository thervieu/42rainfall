binary takes one argument

check if atoi(av[1]) == 0

entirely freads "/home/user/end/.pass" into 'str_read'
puts a '\0' at the index atoi(av[1]) of 'str_read'
compares 'str_read' and av[1]
atoi("") = 0
"\0" = ""

./bonus ""
we are inside a shell

works because "" = "" and atoi("") = 0
