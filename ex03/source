void v() {
    int m;

    char *tmp
    fgets(tmp, strlen(tmp), stdin);
    printf("tmp");
    if (m == 64) {
        fwrite(1, "Wait what?!");
        system("/bin/sh");
    }
}

int main() {
    v()
}